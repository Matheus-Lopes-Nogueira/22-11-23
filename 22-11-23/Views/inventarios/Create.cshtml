@model _22_11_23.Models.inventario

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>inventario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <label>idcliente</label>
            <select name="idcliente" class="form-control">x''
                @foreach (var item in Model.listaclientes)
                {
                    <option value="@item.idcliente">@item.nomecliente</option>
                }

            </select>
            <hr />

            <div class="form-group">
                <label asp-for="idcliente" class="control-label"></label>
                <input asp-for="idcliente" class="form-control" />
                <span asp-validation-for="idcliente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="idmaquina" class="control-label"></label>
                <input asp-for="idmaquina" class="form-control" />
                <span asp-validation-for="idmaquina" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="qtvalor" class="control-label"></label>
                <input asp-for="qtvalor" class="form-control" />
                <span asp-validation-for="qtvalor" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
